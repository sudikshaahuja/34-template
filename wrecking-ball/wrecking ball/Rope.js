class Rope {
  constructor() {
    var options = {
      
    };

    this.pointB = pointB;
    this.rope = Constraint.create(options);
    World.add(world, this.rope);
  }
  attach(body) {
    
  }

  fly() {
    
  }

  display() {
   
  }
}
